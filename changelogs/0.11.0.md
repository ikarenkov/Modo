## New Features

### `ProvideOverlayIntegration` — window overlay support (#72)

New composable extension on `Screen` that properly integrates window-based overlays (`Dialog`, `ModalBottomSheet`, `Popup`, etc.) with Modo's screen lifecycle and state management.

**Problem it solves:** Window-based overlays create a separate `AndroidComposeView` with composition locals inherited from Activity level, not from the Screen. This caused:
- `ViewModel` scoped to Activity instead of Screen
- `rememberSaveable` state lost on back navigation

**Usage:**
```kotlin
ModalBottomSheet(onDismissRequest = { navigation.back() }) {
    ProvideOverlayIntegration {
        MyBottomSheetContent()
    }
}
```

---

## Bug Fixes

- **Fixed crash** when several permanent dialogs were forwarded with a screen, causing wrong lifecycle events order (#75)
- **Fixed `ScreenModelStore` clearing wrong screens** due to key prefix collision — screens with similar key prefixes were incorrectly cleaned up (#73)
- **Fixed custom dialogs transition animation** that was broken in certain cases (#71)
- **Fixed recompositions in sample** caused by counter (#75)

---

## Deprecations

- `Modo.getOrCreateRootScreen` — use `Modo.rememberRootScreen` instead (#76)
- `Modo.save` — use `Modo.rememberRootScreen` instead (#76)
- `Modo.onRootScreenFinished` — use `Modo.rememberRootScreen` instead (#76)

All deprecated APIs will be removed in 1.0.

---

## API Changes

### `SaveableContent` — removed `manualResumePause` parameter (#75)

The `manualResumePause: Boolean` parameter was removed from `SaveableContent`; the transition lifecycle context is now propagated via an internal `CompositionLocal`. If you were passing this parameter explicitly, remove it from your call sites.

---

## Refactoring

- **`ScreenLifecycleManager`** — lifecycle management logic extracted from `ModoScreenAndroidAdapter` into a dedicated class, making lifecycle handling more predictable and testable (#71, #75)

---

## Dependency Updates

| Dependency | Before | After |
|---|---|---|
| Kotlin | `1.9.23` | `2.0.21` |
| AGP | `8.4.0` | `8.13.2` |
| Compose BOM | `2024.03.00` | `2025.11.01` |
| `compileSdk` | `34` | `36` |

---

## Improvements

- Extensive new tests for `ModoScreenAndroidAdapter` and `ScreenModelStore` (#71, #73, #75, #76)
- Added dependency guard plugin to track and control library's public dependencies (#69)
- Migrated publishing to Sonatype Central Portal (#74)

---

## What's Changed

* Added dependency guard plugin to control library dependencies by @ikarenkov in https://github.com/ikarenkov/Modo/pull/69
* Updated gradle, agp, kotlin by @ikarenkov in https://github.com/ikarenkov/Modo/pull/70
* Refactored screens cleanup and lifecycle logic by @ikarenkov in https://github.com/ikarenkov/Modo/pull/71
* Add integration for window-based overlays by @ikarenkov in https://github.com/ikarenkov/Modo/pull/72
* Fix screen model store clear by @ikarenkov in https://github.com/ikarenkov/Modo/pull/73
* Publishing update by @ikarenkov in https://github.com/ikarenkov/Modo/pull/74
* Lifecycle refactoring by @ikarenkov in https://github.com/ikarenkov/Modo/pull/75
* Refactor Modo init: deprecate legacy API, unify caching, added unit tests by @ikarenkov in https://github.com/ikarenkov/Modo/pull/76

**Full Changelog**: https://github.com/ikarenkov/Modo/compare/v0.10.1...v0.11.0
